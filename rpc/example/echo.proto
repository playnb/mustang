syntax = "proto2";

package testrpc;

message RequestEcho{
    optional string text = 1;
}

message ReplyEcho{
    optional string text = 1;
}

service EchoService {
    rpc Echo(RequestEcho) returns (ReplyEcho);
}